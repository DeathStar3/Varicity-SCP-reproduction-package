import * as file from '../db.json';

describe('in core_file test project', () => {
    describe('concerning vp_folder', () => {
        it('parentFolder is', () => { });
        it('folder 2 is not', () => { });
    });
    describe('concernign variant_folder', () => {
        it('folder 1 is', () => { });
        it('folder 2 is', () => { });
        it('folder 3 is', () => { });
    });
    describe('concerning variant_file', () => {
        it('fileA is', () => { });
        it('fileB is', () => { });
        it('fileC is not', () => { });
    });
    describe('concerning super_variant_file', () => {
        it('fileA is not', () => { });
        it('fileB is not', () => { });
        it('fileC is not', () => { });
    });
});